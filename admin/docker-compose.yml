services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://isjdiokkdm:2gKL3CDyGnEdMV04uRu1@5.42.96.211:5435/run
      SECRET_KEY: change-me-in-production
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: adminpassword
      JWT_EXPIRE_MINUTES: "1440"
    expose:
      - "8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
